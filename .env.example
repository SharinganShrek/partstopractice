# YouTube Data API v3 key (https://console.cloud.google.com/apis/credentials)
# Enables live “people reached” total from summed video view counts on the home page.
YOUTUBE_API_KEY=

# Optional: how long to cache totals server-side (seconds). Default 3600 (1 hour).
# YOUTUBE_VIEWS_REVALIDATE_SECONDS=3600
